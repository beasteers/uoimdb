{% extends 'base.j2' %}

{% block nav_brand %}
  {{ super() }}
  <small class='badge badge-light'>sample: {{ name }}</small>
{% endblock %}

{% block nav_content %}
    <div class="collapse navbar-collapse"></div>

    {% if links %}
      <div class="dropup show">
        <a class="btn btn-outline-dark dropdown-toggle" href="#" role="button" id="selectSample" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Choose Sample
        </a>

        <div class="dropdown-menu" aria-labelledby="selectSample">
        {% for link in links %}
          <a class="dropdown-item {{ 'active' if link.name == name }}" href="{{ link.url }}">{{ link.name }}</a>
        {% endfor %}
        </div>
      </div>
    {% endif %}
{% endblock %}


{% block content %}
	<table class="table text-light">
    <thead>
      <tr>
        {% for c in columns %}
          <th scope="col">{{ c }}</th>
        {% endfor %}
      </tr>
    </thead> 
    <tbody>
      {% for row in data %}
        <tr>
          {% for c in columns %}
            <td>{{ row[c] }}</td>
          {% endfor %}
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% endblock %}

{% block scripts %}{% endblock %}

